name: pedometer
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  # GetX：路由 + 状态管理（对齐 chat）
  get: ^4.6.6
  # 数字 / 日期格式化与国际化
  intl: ^0.20.2
  # 屏幕尺寸适配（设计稿基准 375x812）
  flutter_screenutil: ^5.9.3
  # 折线图 / 面积图（趋势、卡路里、活动时间分析）
  fl_chart: ^0.69.0
  # 运动完成礼花效果
  flutter_confetti: ^0.5.1
  # Google 地图组件
  google_maps_flutter: ^2.17.1
  # 高精度定位与运行时位置权限
  geolocator: ^14.0.2
  # 设备磁力计罗盘朝向（当前位置箭头随手机方向旋转）
  flutter_compass: ^0.8.1
  # 本地健康数据同步：iOS Apple Health / Android Health Connect
  health: ^13.3.1
  # 系统分享面板（分享应用）
  share_plus: ^11.0.0
  # SVG 图标资源
  flutter_svg: ^2.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  fake_async: ^1.3.2
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/common/resources/
    - lib/feature/home/resources/
    - lib/feature/workout/resources/
    - lib/products/phone/resources/
    - assets/
    - assets/images/
